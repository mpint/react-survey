app loads
  looks in localstorage for userid
    if userid,
      load questions that dont contain userid
    else
      set userid in localstorage

answer question
  updates question with userid

title = 'asdsaee'
responses = ['asdfa', 'gdsaadsf', 'grege', 'zcxzc']
responseCount: [5, 4, 6, 1]
